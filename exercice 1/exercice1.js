/* Modifier le code ci dessous pour réaliser l'exercice */

let x;
x = 10;
let y;
y = 15;

    if (x > y) {
        document.getElementById('monDiv').innerHTML = "La variable x est supérieur à la variable y";
        alert("ce n'est pas bon");
    }
    else {
        document.getElementById('monDiv').innerHTML = "La variable x est supérieur à la variable y";
    }

